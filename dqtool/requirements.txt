# Core Dependencies
python-dotenv==1.0.0

# LLM and AI
litellm==1.48.7

# Database
# sqlite3 is included in Python standard library

# YAML Processing
pyyaml==6.0.1

# Logging
# logging is included in Python standard library

# Additional dependencies that might be needed by litellm
requests==2.31.0
openai==1.3.0
anthropic==0.7.0

# For fuzzy matching (difflib is in standard library, but these are alternatives if needed)
# python-Levenshtein==0.23.0
# fuzzywuzzy==0.18.0

# Type hints support
typing-extensions==4.8.0

# Core dependencies
pandas>=2.0.0
openpyxl>=3.1.0  # For Excel file support
python-dotenv>=1.0.0
pyyaml>=6.0

# LLM dependencies (adjust based on your llm_config.py)
google-generativeai>=0.3.0  # For Gemini API

# Database
# sqlite3 is included in Python standard library

# Optional but recommended
xlrd>=2.0.0  # For reading older .xls files
